export interface TokenData {
  address: string;
  chainId: string;
  id: string;
  image: string;
  coinGeckoID: string;
  name: string;
  symbol: string;
  priceUsd: string;
  dailyPercentChange: string;
  balance: string;
}
